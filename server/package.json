{
  "name": "bubbles",
  "version": "1.0.0",
  "description": "A project to... efficiently manage all projects.",
  "main": "main.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "deno run --allow-env --allow-sys --watch --allow-write --allow-read --allow-net main.ts",
    "build": "deno run --allow-env --allow-sys --unstable main.ts"
  },
  "author": "",
  "license": "ISC"
}
